
syntax = "proto3"; // "proto2" is also supported.

package org.example.protos;

option java_multiple_files = true;

message AppEvent {
    string name = 1;
    int64 id = 2;
    string context = 3;
}
